// Tailwind CSS v4 uses CSS-first configuration.
// The custom theme (colors, fonts, etc.) is defined in src/styles/global.css
// inside the @theme {} block. This file is kept for IDE tooling compatibility.
export default {};
